specVersion: "v1beta"
serviceId: "to-do-Liste"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "to-do-Liste-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
